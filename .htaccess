Options -Indexes
DirectoryIndex index.php

<IfModule mime_module>
  AddHandler application/x-httpd-ea-php84 .php .php8 .phtml
</IfModule>

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /45/

  # Serve HTML pages from public/ without changing the URL
  RewriteRule ^(game|lobby|admin|player)\.html$ public/$1.html [L]

  # Serve player-modal.js from public/
  RewriteRule ^player-modal\.js$ public/player-modal.js [L]

  RewriteCond %{REQUEST_URI} !^/45/public/
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.php [QSA,L]
</IfModule>
